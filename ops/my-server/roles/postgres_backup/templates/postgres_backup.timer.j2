[Unit]
Description=Daily PostgreSQL backup

[Timer]
OnCalendar=*-*-* {{ '%02d' | format(postgres_backup_config.hour) }}:00:00
RandomizedDelaySec=1800
Persistent=true

[Install]
WantedBy=timers.target

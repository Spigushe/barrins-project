"""General routes — not specific to any versioned or BFF-namespaced domain.

Currently: the root redirect, the /health check, and v1 auth (kept here
rather than in its own package while auth is v1's only content).
"""

# Barrin's Identity origin this shell calls directly (cookie mode, ADR-18).
# Must be a bare origin (scheme + host, no path). That deployment needs
# REFRESH_COOKIE_ENABLED=true and this app's origin in ALLOWED_ORIGINS.
VITE_IDENTITY_SERVICE_URL=http://localhost:8001
